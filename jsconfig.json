{
  "name": "svelte-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0", // Adjust the version as needed
    "svelte": "^3.0.0", // Adjust the version as needed
    "typescript": "^4.0.0", // Adjust the version as needed
    "vite": "^2.0.0", // Adjust the version as needed
    "svelte-preprocess": "^4.0.0" // Adjust the version as needed
  },
  "dependencies": {
    // Add other dependencies here if needed
  }
}
